// Stub file for wagmi config - replaced with Solana
// This file exists to prevent import errors in components that haven't been migrated yet

export const wagmiConfig = null as any;

export const publicClient = null as any;

export const getGasPrice = async () => {
  return BigInt(0);
};
